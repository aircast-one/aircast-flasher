//! The desktop entry point and all Tauri commands live in `main.rs`.
//!
//! This `lib` target exists only because the Tauri project layout declares one
//! (and to keep the door open for a future mobile `mobile_entry_point`). It is
//! intentionally empty for the desktop flasher.
